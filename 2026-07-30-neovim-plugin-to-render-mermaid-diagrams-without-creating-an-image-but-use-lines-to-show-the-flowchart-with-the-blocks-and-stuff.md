# Neovim plugin to render mermaid diagrams without creating an image but use lines to show the flowchart (WIth the blocks and stuff)

## 1. Project Base Idea

So normally if I have to render a mermaid diagram in `nvim`, I need to use a terminal like `wezterm` and `image.nvim`. What if I could view the mermaid diagram without needing these and it is rendered as text instead of an image.


## 2. Project Idea in Detail

So normally if I have to render a mermaid diagram in `nvim`, I need to use a terminal like `wezterm` and `image.nvim`. What if I could view the mermaid diagram without needing these and it is rendered as text instead of an image.

This will be pretty helpful in my opinion


## 3. Programming Language(s) Planned to Use

Lua, Rust (Maybe)

## 4. Tech Stack Planned

No Idea. First time creating a `nvim` plugin 

## 5. Additional Notes

1. [`txm.nvim`](https://github.com/rv178/txm.nvim)
